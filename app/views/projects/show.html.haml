- title(@project.name, 'Projects')
%h2= @project.name
%p
  =link_to 'Edit Project', edit_project_path(@project)
  %br
  =link_to 'Delete Project', @project, :method => :delete, :confirm => 'Are your sure you want to delete this project?'
  %br
  =link_to 'New Ticket', new_project_ticket_path(@project)
%p= link_to 'All Projects', projects_path
